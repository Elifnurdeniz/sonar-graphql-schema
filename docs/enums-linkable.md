```graphql
"""A type of entity that can be linked to a `Ticket`."""
enum TicketLinkableEntityType {
  """A `Job`."""
  Job
  """An `InventoryItem`."""
  InventoryItem
  """A `Network Site`."""
  NetworkSite
}
```
